# Interfaces

How users interact with the testing system.

| Sub-folder | What | Status |
|---|---|---|
| `cli/` | Command-line entrypoint that orchestrates the full pipeline | scaffold |
| `web-ui/` | Browser dashboard (read-only view of knowledge-base + tests) | deferred |

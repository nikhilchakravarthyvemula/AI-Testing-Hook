# The single shared venv for the pipeline Python stack + the MCP servers.
# Built at _lib/.venv and used by: run.mjs code-extractors, db-schema,
# framework-detector, skill-register (call_skill.py), the api-test-generator
# skill, the graphify tool, and mcp-server/ai_hook_mcp + byo-llm-poc/mcp_server.py.
#
# BYO-LLM: no LLM SDKs here — the host model does all reasoning via MCP
# sampling. graphifyy's LLM extras are deliberately NOT installed; its core
# (update/tree) is deterministic (networkx, tree-sitter, numpy, rapidfuzz).
graphifyy            # deterministic knowledge-graph CLI (update/tree)
mcp>=1.0             # mcp-server/ai_hook_mcp + byo-llm-poc/mcp_server.py (FastMCP + sampling)
pydantic>=2.7,<3     # skill_register, framework_extractor, code_extractors, testing_agent, api-test-generator
pyyaml>=6.0,<7       # testing_agent openapi_file extractor
requests>=2.31       # api-test-generator login flow (was transitive via openharness — now explicit)
rich>=14.2           # testing_agent platform logging

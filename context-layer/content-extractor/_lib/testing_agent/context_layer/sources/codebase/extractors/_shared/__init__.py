"""Helpers used across multiple extractors.

  * `selectors.py`       — the four-tier selector ladder (testid > role > label > text)
  * `backend_common.py`  — path normalization, route joining, auth markers
  * `file_walking.py`    — common file-traversal helpers
  * `provenance.py`      — provenance construction helpers
"""

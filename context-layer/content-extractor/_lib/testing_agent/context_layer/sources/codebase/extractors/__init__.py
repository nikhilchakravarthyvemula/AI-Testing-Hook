"""Extractor implementations, grouped by what they extract from.

  * `backend/`  — server-side framework endpoint extraction
  * `frontend/` — UI framework route + interaction extraction
  * `specs/`    — declared spec parsing (OpenAPI, markdown)
  * `facts/`    — codebase facts (package manifests, conventions)
  * `_shared/`  — helpers used across multiple extractors
"""

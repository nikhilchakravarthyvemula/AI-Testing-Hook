
# secrets
.env
.env.backup.*

# build / deps / caches
node_modules/
__pycache__/
*.pyc
.venv/
.DS_Store

# generated crawl/test artifacts (regenerable)
output/

# generated Playwright specs - e2e.mjs wipes _ rewrites these every run.
# The scaffold (tests/playwright.config.mjs, tests/helpers/**) IS committed.
tests/e2e/

# local harness config
.claude/settings.local.json

# Confidential HSBC engagement files — never commit
SDD - AI Testing Harness*.docx
Interview Guide*.docx
hsbc-followup-email.md
*.docx

# per-user source connections (internal hostnames; PATs live in the OS secret store)
.testo/
